// IMPORTANT!
// Hello, before you start creating your style, please check the https://copan.loft.com.br/ to see if it doesn't meet all your needs.
// If this is a new component, you can contribute with your needs https://copan.loft.com.br/how-to-contribute
// If this is a customization to an existing component, please follow up with your designer. We shouldn’t have this scenario.

/*
import { spacing } from '@loft/copan-tokens';
import { makeStyles } from '@material-ui/core';
import { {{pascalCase name}}Props } from './{{pascalCase name}}.component';

export default makeStyles({
  root: (props: {{pascalCase name}}Props) => ({
    margin: spacing['09'],
    padding: props.x ? 1 : 2,
  }),
});
*/
